---
import MainLayout from '../layouts/MainLayout.astro';
import PuzzleDisplay from '../components/PuzzleDisplay.astro';
import { fetchLatestMiniPuzzle, CROSSWORD_SOURCES } from '../lib/api';

const source = CROSSWORD_SOURCES.find(s => s.slug === 'nyt-mini')!;
let puzzle = null;
let error = '';

try {
  puzzle = await fetchLatestMiniPuzzle();
  if (!puzzle) error = 'No mini puzzle found for today. Check back later!';
} catch (e: any) {
  error = e.message || 'Failed to fetch mini puzzle data.';
}

---

<MainLayout
  title="NYT Mini Crossword Answers Today"
  description="Today's NYT Mini Crossword answers and complete solutions. Quick answers for the New York Times Mini crossword puzzle."
>
  <section class="page-section">
    <div class="container">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/">Home</a>
        <span class="bc-sep">/</span>
        <span>NYT Mini Crossword</span>
      </nav>

      {puzzle ? (
        <PuzzleDisplay
          date={puzzle.date}
          formattedDate={puzzle.formattedDate}
          title={puzzle.title || 'NYT Mini Crossword Answers Today'}
          author={puzzle.author}
          editor={puzzle.editor}
          dayOfWeek={puzzle.dayOfWeek}
          across={puzzle.across}
          down={puzzle.down}
          showAnswers={true}
          sourceColor={source.color}
          archiveLink="/archive"
        />
      ) : (
        <div class="error-state">
          <div class="error-icon">
            <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
              <circle cx="24" cy="24" r="22" stroke="#d93025" stroke-width="2"/>
              <path d="M24 16v12M24 32v2" stroke="#d93025" stroke-width="2" stroke-linecap="round"/>
            </svg>
          </div>
          <h2 class="error-title">Unable to Load Mini Puzzle</h2>
          <p class="error-msg">{error}</p>
          <a href="/" class="btn btn-secondary">Back to Home</a>
        </div>
      )}

      <div class="content-section">
        <h2 class="content-heading">About the NYT Mini Crossword</h2>
        <p class="content-text">
          The New York Times Mini Crossword is a compact 5x5 grid puzzle introduced in 2014, designed to be completed in just a few minutes. Despite its small size, the Mini packs a punch with clever clues that range from straightforward to surprisingly tricky. It has become incredibly popular as a quick daily mental exercise, often solved during commutes or coffee breaks by millions of puzzle enthusiasts.
        </p>
        <div class="content-links">
          <a href="/archive" class="btn btn-ghost">Browse NYT Mini Archive</a>
          <a href="/crossword-solver" class="btn btn-ghost">Open Crossword Solver</a>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  .page-section { padding: 24px 0 48px; }
  .breadcrumb { display: flex; align-items: center; gap: 6px; margin-bottom: 20px; font-size: 0.82rem; color: var(--color-text-muted); }
  .breadcrumb a { color: var(--color-text-muted); }
  .breadcrumb a:hover { color: var(--color-accent); }
  .bc-sep { color: var(--color-border); }
  .error-state { text-align: center; padding: 56px 24px; max-width: 480px; margin: 0 auto; background: var(--color-bg-card); border-radius: var(--radius-lg); border: 1px solid var(--color-border); }
  .error-icon { margin-bottom: 16px; }
  .error-title { font-size: 1.3rem; font-weight: 700; margin-bottom: 8px; }
  .error-msg { color: var(--color-text-muted); margin-bottom: 20px; line-height: 1.6; font-size: 0.9rem; }
  .content-section { margin-top: 40px; max-width: 760px; margin-left: auto; margin-right: auto; }
  .content-heading { font-size: 1.3rem; font-weight: 700; margin-bottom: 14px; }
  .content-text { color: var(--color-text-secondary); font-size: 0.92rem; line-height: 1.8; margin-bottom: 14px; }
  .content-links { display: flex; gap: 8px; margin-top: 20px; flex-wrap: wrap; }
</style>
